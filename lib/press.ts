
export namespace press {
    export const style = require( './style' ).style;
    export const palette = require( './palette' ).palette;
    export const wire = require( './wire').wire;
    export const util = require( './util').util;
}
